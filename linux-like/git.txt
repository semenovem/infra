
# long path and line feed support
git config --global core.autocrlf input

# default strategy
git config --global pull.rebase true

#"git push" будет пушить только текушую ветку
git config push.default simple

git config user.name "Evgeny Semenov"
git config user.email "emsemenov@gmail.com"

git config --global init.defaultBranch main

# добавить первичную инициализацию
# .gitignore

[user]
        name = Semenov Evgeny
        email = semenovem@gmail.com
[core]
        autocrlf = input
[pull]
        rebase = true
[init]
        defaultBranch = main
